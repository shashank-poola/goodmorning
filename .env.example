# Set to true once server/.env has Google credentials and server is running.
# Enables real Calendar + Gmail data via the backend.
VITE_USE_API=false

# Leave empty in dev — Vite proxies /api and /auth to localhost:3001
VITE_API_BASE_URL=
