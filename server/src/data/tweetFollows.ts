/**
 * X handles to follow — keep in sync with src/data/tweetFollows.ts.
 * Add / remove / reorder handles here; the RSS service pulls the latest post per handle.
 */
export const TWEET_FOLLOWS: string[] = [
  '@paulg',
  '@AnthropicAI',
  '@naval',
  '@sama',
  '@karpathy',
]
